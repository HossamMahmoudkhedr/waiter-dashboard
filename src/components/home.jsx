import { Box } from '@mui/material';
import React from 'react';
import Heading from '../utils/heading';

const Home = () => {
	return (
		<Box height="70vh">
			<Heading text='إضغط على "صانع التطبيقات" من القائمة' />
		</Box>
	);
};

export default Home;
